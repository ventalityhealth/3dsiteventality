/**
 * VineGrowthScene — placeholder scene.
 *
 * Lightweight stub so the page composes end-to-end. Replace with the real
 * vine-growth scene. CSS-only visuals (no SVG / vector graphics).
 */
export default function VineGrowthScene() {
  return (
    <section className="relative flex h-[100dvh] w-full items-center justify-center overflow-hidden bg-gradient-to-b from-[#050505] via-[#0d120c] to-[#050505]">
      <div className="text-center">
        <h2 className="text-3xl font-medium tracking-tight text-emerald-100/80 md:text-5xl">
          Grown, not synthesized
        </h2>
        <p className="mt-4 text-sm text-white/40">Vine growth scene placeholder</p>
      </div>
    </section>
  );
}
